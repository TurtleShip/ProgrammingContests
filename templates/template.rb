#!/usr/bin/ruby

# Get one number at a time
N = STDIN.gets.to_i
P = STDIN.gets.to_i

# Get two numbers at once
# You can also save it as an array
r,c = gets.strip.split.map {|num| num.to_i}

puts sum
